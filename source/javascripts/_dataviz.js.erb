$( document ).ready(function() {

  var dataset_samsung               = <%= data.flow_samsung.to_json %>
  var dataset_samsung_onboarding    = <%= data.flow_samsung_onboarding.to_json %>

  app.dataviz = new Viz(dataset_samsung, {
    'target': ".container",
    'label': false,
    'image': false,
    'depth': 80,
    'collapse_nodes': true
  })

  app.dataviz.init()

  console.log(app.dataviz.data)


  // d3.select(self.frameElement).style("height", "800px");

});
